package StepDefinitions;

import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import Pages.LoginPage;
import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginSteps {

	/*
	 * WebDriver driver = null; LoginPage login;
	 * 
	 * @Given("user is on login page") public void user_is_on_login_page() { String
	 * projectPath = System.getProperty("user.dir");
	 * System.setProperty("webdriver.chrome.driver",projectPath+
	 * "/src/test/resources/Drivers/chromedriver.exe"); System.out.println(); driver
	 * = new ChromeDriver();
	 * driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(30));
	 * driver.manage().timeouts().pageLoadTimeout(Duration.ofSeconds(30));
	 * driver.navigate().to("https://example.testproject.io/web/");
	 * 
	 * }
	 * 
	 * @When("user enter(.*)and(.*)$") public void
	 * user_enter_username_and_password(String username,String password) throws
	 * InterruptedException {
	 * 
	 * login = new LoginPage(driver); login.enterUsername(username);
	 * login.enterPassword(password);
	 * //driver.findElement(By.id("name")).sendKeys(username);
	 * //driver.findElement(By.id("password")).sendKeys(password);
	 * Thread.sleep(2000);
	 * 
	 * }
	 * 
	 * @And("clicks on login button") public void clicks_on_login_button() throws
	 * InterruptedException { login = new LoginPage(driver); login.clickLogin();
	 * //driver.findElement(By.id("login")).click(); Thread.sleep(2000);
	 * 
	 * }
	 * 
	 * @Then("navigated to home page") public void navigated_to_home_page() throws
	 * InterruptedException { driver.getPageSource().contains("logout");
	 * Thread.sleep(2000); driver.close(); driver.quit();
	 * 
	 * }
	 */
}
